# Auto-generated from implementation.py
PART = 'import time\nimport streamlit as st\nfrom core.ui.compact import render_metric_cards, render_section_note\nimport pandas as pd\nimport numpy as np\n\ntry:\n    from core.ui_helpers import choice_buttons, sound_alert, recent_signal_caption, set_recent_signal, collapsible_text\nexcept Exception:\n    def choice_buttons(label, options, key, columns=4, default=None, help_text=""):\n        import streamlit as st\n        options=list(options or [])\n        if not options:\n            return ""\n        return st.radio(label, options, key=key)\n    def sound_alert(*args, **kwargs):\n        return False\n    def recent_signal_caption(*args, **kwargs):\n        return ""\n    def set_recent_signal(signal_key):\n        import pandas as pd, streamlit as st\n        st.session_state[signal_key] = pd.Timestamp.now().strftime("%Y-%m-%d %H:%M:%S")\n    def collapsible_text(title, text, expanded=False, threshold_words=10):\n        import streamlit as st\n        with st.expander(title, expanded=expanded):\n            st.write(text)\n\n\n# ==========================================================\n# SAFE IMPORTS\n# ==========================================================\n\ntry:\n    from core.common import DEFAULT_TABS, log_event\nexcept Exception:\n    DEFAULT_TABS = ["home", "engine", "backtest", "guide", "account"]\n\n    def log_event(*args, **kwargs):\n        return None\n\ntry:\n    from core.styles import request_close_sidebar\nexcept Exception:\n    def request_close_sidebar(*args, **kwargs):\n        return None\n\ntry:\n    from core.data_connectors import manual_connect, mt5_account_info, get_mt5_account_snapshot\nexcept Exception:\n    manual_connect = None\n    mt5_account_info = None\n\ntry:\n    from core.quant_models import quant_stack\nexcept Exception:\n    quant_stack = None\n\ntry:\n    from core.database import append_csv, read_csv\nexcept Exception:\n    append_csv = None\n    read_csv = None\n\ntry:\n    from ..doo_prime_deep import doo_prime_deep_analysis_panel\nexcept Exception:\n    def doo_prime_deep_analysis_panel():\n        st.error("Doo Prime deep analysis module could not be loaded.")\n\n\n\n# ==========================================================\n# SAFE HELPERS\n# ==========================================================\n\ndef _safe_num(v, default=0.0):\n    try:\n        if v is None:\n            return default\n        if isinstance(v, str) and v.strip() == "":\n            return default\n        v = float(v)\n        if not np.isfinite(v):\n            return default\n        return v\n    except Exception:\n        return default\n\n\ndef _safe_int(v, default=0):\n    try:\n        if v is None:\n            return default\n        return int(v)\n    except Exception:\n        return default\n\n\ndef _safe_text(v, default=""):\n    try:\n        if v is None:\n            return default\n        return str(v)\n    except Exception:\n        return default\n\n\ndef _safe_append_csv(name, row):\n    if append_csv is None:\n        return False, "append_csv is unavailable."\n\n    try:\n        append_csv(name, row)\n        return True, "Saved."\n    except Exception as exc:\n        return False, f"Save failed: {exc}"\n\n\ndef _safe_read_csv(name):\n    if read_csv is None:\n        return pd.DataFrame()\n\n    try:\n        df = read_csv(name)\n        if df is None:\n            return pd.DataFrame()\n        return df\n    except Exception:\n        return pd.DataFrame()\n\n\ndef _safe_rerun():\n    try:\n        st.rerun()\n    except Exception:\n        try:\n            st.experimental_rerun()\n        except Exception:\n            pass\n\n\ndef _safe_log_event(message):\n    try:\n        log_event(message)\n    except Exception:\n        pass\n\n\ndef _safe_close_sidebar():\n    try:\n        request_close_sidebar()\n    except Exception:\n        pass\n\n\ndef _normalize_account_info(raw):\n    """\n    Accepts different possible MT5 connector outputs and returns one safe dict.\n\n    Supported:\n    - dict account snapshot\n    - tuple: info, ok, msg\n    - tuple: ok, info, msg\n    """\n    if raw is None:\n        return {}, False, "No MT5 account data returned."\n\n    if isinstance(raw, tuple):\n        if len(raw) >= 3:\n            a, b, c = raw[0], raw[1], raw[2]\n\n            if isinstance(a, dict):\n                return a, bool(b), _safe_text(c, "MT5 account read finished.")\n\n            if isinstance(b, dict):\n                return b, bool(a), _safe_text(c, "MT5 account read finished.")\n\n        if len(raw) >= 1 and isinstance(raw[0], dict):\n            return raw[0], True, "MT5 account read finished."\n\n    if isinstance(raw, dict):\n        ok = bool(raw.get("ok", True))\n        msg = raw.get("message", "MT5 account read finished.")\n        return raw, ok, msg\n\n    return {}, False, "Unsupported MT5 account response format."\n\n\ndef _safe_mt5_account_info():\n    try:\n        # If Doo Bridge is selected in the sidebar, read account/positions through bridge.\n        # Otherwise read direct local MT5 account.\n        if st.session_state.get("connector_mode") == "doo_bridge" and st.session_state.get("doo_bridge_url"):\n            snap = get_mt5_account_snapshot(\n                bridge_url=st.session_state.get("doo_bridge_url", ""),\n                bridge_token=st.session_state.get("doo_bridge_token", ""),\n            )\n            info = dict(snap.get("account", {}) or {})\n            info["positions"] = snap.get("positions", []) or []\n            return info, bool(snap.get("ok")), snap.get("message", "Doo Bridge account read finished.")\n\n        if mt5_account_info is None:\n            return {}, False, "mt5_account_info is unavailable. Check core.data_connectors."\n\n        raw = mt5_account_info()\n        info, ok, msg = _normalize_account_info(raw)\n        return info, ok, msg\n    except Exception as exc:\n        return {}, False, f"Account reader crashed safely: {exc}"\n\n\ndef _safe_manual_connect(source, symbol, api_key="", bars=5000, timeframe="M1"):\n    if manual_connect is None:\n        st.error("manual_connect is unavailable. Check core.data_connectors import.")\n        return\n\n    try:\n        with st.spinner(f"Connecting {source.upper()} {symbol} {timeframe}..."):\n            manual_connect(\n                source,\n                symbol,\n                api_key,\n                bars=bars,\n                timeframe=timeframe,\n            )\n\n        st.success(f"Connected: {source.upper()} {symbol} {timeframe}")\n        _safe_rerun()\n\n    except Exception as exc:\n        st.error(f"{source.upper()} connection failed safely: {exc}")\n\n\ndef _safe_quant_stack(df):\n    if df is None:\n        return {\n            "bias": "WAIT",\n            "scale10": 0,\n            "safe_pct": 0,\n        }\n\n    if quant_stack is None:\n        return {\n            "bias": "WAIT",\n            "scale10": 0,\n            "safe_pct": 0,\n            "message": "quant_stack unavailable",\n        }\n\n    try:\n        q = quant_stack(\n            df,\n            st.session_state.get("trade_history", []),\n            st.session_state.get("account_snapshot", {}),\n        )\n\n        if not isinstance(q, dict):\n            return {\n                "bias": "WAIT",\n                "scale10": 0,\n                "safe_pct": 0,\n                "message": "quant_stack returned invalid output",\n            }\n\n        q.setdefault("bias", "WAIT")\n        q.setdefault("scale10", 0)\n        q.setdefault("safe_pct", 0)\n\n        return q\n\n    except Exception as exc:\n        return {\n            "bias": "WAIT",\n            "scale10": 0,\n            "safe_pct": 0,\n            "message": f"quant_stack crashed safely: {exc}",\n        }\n\n\ndef _save_once_per_60_seconds(name, row, state_key):\n    """\n    Prevent duplicate CSV spam on every Streamlit rerun.\n    """\n    now = pd.Timestamp.now()\n    last_time = st.session_state.get(state_key)\n\n    should_save = False\n\n    if last_time is None:\n        should_save = True\n    else:\n        try:\n            elapsed = (now - pd.to_datetime(last_time)).total_seconds()\n            should_save = elapsed >= 60\n        except Exception:\n            should_save = True\n\n    if should_save:\n        ok, msg = _safe_append_csv(name, row)\n        if ok:\n            st.session_state[state_key] = now\n        return ok, msg\n\n    return True, "Skipped duplicate auto-save."\n\n\n\ndef _auto_save_account_snapshot(info, positions_df=None, reason="auto"):\n    """Auto-save account state once per 60 seconds when a real result exists."""\n    if not isinstance(info, dict) or not info:\n        return False, "No account info to auto-save."\n    positions = info.get("positions", []) or []\n    row = {\n        "time": pd.Timestamp.now(),\n        "reason": reason,\n        "symbol": st.session_state.get("symbol", "XAUUSD"),\n        "balance": _safe_num(info.get("balance")),\n        "equity": _safe_num(info.get("equity")),\n        "margin": _safe_num(info.get("margin")),\n        "margin_free": _safe_num(info.get("margin_free")),\n        "margin_level": _safe_num(info.get("margin_level")),\n        "profit": _safe_num(info.get("profit")),\n        "positions": len(positions_df) if isinstance(positions_df, pd.DataFrame) and not positions_df.empty else len(positions),\n    }\n    return _save_once_per_60_seconds("doo_prime_account_history", row, "doo_account_last_auto_save_time")\n\n\ndef _refresh_shared_market_data_from_here(label="Doo Prime refresh"):\n    if manual_connect is None:\n        st.error("manual_connect is unavailable.")\n        return\n    current_main_tab = st.session_state.get("tab_choice", "Home")\n    current_home_section = st.session_state.get("home_lazy_section", "🏦 Doo Prime")\n    current_doo_section = st.session_state.get("doo_prime_lazy_section_home", "🏦 Real Account Stats")\n    try:\n        with st.spinner(f"{label}: refreshing shared market dataframe without changing your open tab..."):\n            df, ok, source, msg = manual_connect(\n                mode=st.session_state.get("connector_mode", "mt5"),\n                symbol=st.session_state.get("symbol", "XAUUSD"),\n                api_key=st.session_state.get("twelve_api_key", ""),\n                bars=int(st.session_state.get("connector_bars", 600) or 600),\n                timeframe=st.session_state.get("timeframe", "M1"),\n                bridge_url=st.session_state.get("doo_bridge_url", ""),\n                bridge_token=st.session_state.get("doo_bridge_token", ""),\n                allow_demo=bool(st.session_state.get("allow_safe_demo", False)),\n            )\n        st.session_state.tab_choice = current_main_tab\n        st.session_state.home_lazy_section = current_home_section\n        st.session_state.doo_prime_lazy_section_home = current_doo_section\n        if ok:\n            st.success(f"{source}: refreshed {len(df):,} candles. Your open tab/inner section is preserved.")\n        else:\n            st.warning(str(msg))\n    except Exception as exc:\n        st.session_state.tab_choice = current_main_tab\n        st.session_state.home_lazy_section = current_home_section\n        st.session_state.doo_prime_lazy_section_home = current_doo_section\n        st.error(f"Refresh failed safely: {exc}")\n\n\ndef _save_exit_decision_once(action, overview, scenarios, market=None):\n    if not isinstance(action, dict) or not action:\n        return\n    row = {\n        "time": pd.Timestamp.now(),\n        "symbol": st.session_state.get("symbol", "XAUUSD"),\n        "action_short": action.get("action_short", ""),\n        "action": action.get("action", ""),\n        "confidence": action.get("confidence", ""),\n        "reason": action.get("reason", ""),\n        "margin_level": _safe_num((overview or {}).get("current_margin_level")),\n        "danger_gap_pct": _safe_num((overview or {}).get("danger_gap_pct")),\n        "market_direction": (market or {}).get("trend_direction", ""),\n        "dve": _safe_num((market or {}).get("directional_efficiency")),\n        "rising_eff": _safe_num((market or {}).get("efficiency_rising")),\n        "falling_eff": _safe_num((market or {}).get("efficiency_falling")),\n        "scenario_count": len(scenarios) if isinstance(scenarios, pd.DataFrame) else 0,\n    }\n    _save_once_per_60_seconds("emergency_exit_decisions", row, "emergency_exit_decision_last_auto_save_time")\n\n\ndef _handle_exit_action_sound(action, overview=None, market=None):\n    """Immediate sound when the emergency matrix outputs EXIT BUY or EXIT SELL."""\n    short = str((action or {}).get("action_short", "")).upper()\n    if "EXIT SELL" in short:\n        set_recent_signal("last_exit_sell_signal_time")\n        _safe_append_csv("doo_exit_alerts", {\n            "time": pd.Timestamp.now(),\n            "symbol": st.session_state.get("symbol", "XAUUSD"),\n            "signal": "EXIT SELL",\n            "source": "emergency_matrix",\n            "margin_level": _safe_num((overview or {}).get("current_margin_level")),\n            "direction": (market or {}).get("trend_direction", ""),\n            "dve": _safe_num((market or {}).get("directional_efficiency")),\n            "reason": (action or {}).get("reason", ""),\n        })\n        sound_alert(\n            "emergency_exit_sell",\n            "🔔 EXIT SELL result detected",\n            "Emergency matrix currently favors reducing/closing SELL first. This is an alert only; it does not place orders.",\n            severity="danger",\n        )\n    elif "EXIT BUY" in short:\n        set_recent_signal("last_exit_buy_signal_time")\n        _safe_append_csv("doo_exit_alerts", {\n            "time": pd.Timestamp.now(),\n            "symbol": st.session_state.get("symbol", "XAUUSD"),\n            "signal": "EXIT BUY",\n            "source": "emergency_matrix",\n            "margin_level": _safe_num((overview or {}).get("current_margin_level")),\n            "direction": (market or {}).get("trend_direction", ""),\n            "dve": _safe_num((market or {}).get("directional_efficiency")),\n            "reason": (action or {}).get("reason", ""),\n        })\n        sound_alert(\n            "emergency_exit_buy",\n            "🔔 EXIT BUY result detected",\n            "Emergency matrix currently favors reducing/closing BUY first. This is an alert only; it does not place orders.",\n            severity="danger",\n        )\n\n\ndef _render_recent_exit_signal_notes():\n    buy_note = recent_signal_caption("last_exit_buy_signal_time", max_hours=21)\n    sell_note = recent_signal_caption("last_exit_sell_signal_time", max_hours=21)\n    if buy_note or sell_note:\n        with st.expander("🔔 Open recent EXIT BUY / EXIT SELL signal memory", expanded=False):\n            if buy_note:\n                st.write(f"**EXIT BUY:** {buy_note}")\n            if sell_note:\n'
