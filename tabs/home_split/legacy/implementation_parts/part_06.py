# Auto-generated from implementation.py
PART = '        margin_per_001 = st.number_input(\n            "Margin needed per 0.01 lot",\n            min_value=1.0,\n            value=150.0,\n            step=10.0,\n            key="doo_margin_per_001",\n        )\n\n    with h3:\n        planned_entries = st.number_input(\n            "Planned entries",\n            min_value=1,\n            value=1,\n            step=1,\n            key="doo_planned_entries",\n        )\n\n    need = margin_per_001 * (lot / 0.01) * planned_entries\n    possible = int(free / (margin_per_001 * (lot / 0.01))) if lot and margin_per_001 else 0\n    after_plan_free = free - need\n\n    z = st.columns(3)\n    z[0].metric("Margin Needed", round(need, 2))\n    z[1].metric("Possible Entries", possible)\n    z[2].metric("After-Plan Free Margin", round(after_plan_free, 2))\n\n    if after_plan_free < 0:\n        st.error("Planned entries need more margin than current free margin.")\n    elif after_plan_free < free * 0.25:\n        st.warning("Plan leaves low free margin. Consider smaller lot or fewer entries.")\n    else:\n        st.success("Plan is acceptable by this margin helper.")\n\n\n# ==========================================================\n# RISK PANEL\n# ==========================================================\n\ndef risk_panel():\n    st.markdown("### 🛡️ Risk Inner Tab — under Doo Prime only")\n\n    acct = st.session_state.get("account_snapshot", {})\n\n    balance = _safe_num(acct.get("balance"), 1000.0)\n    equity = _safe_num(acct.get("equity"), balance)\n\n    c1, c2, c3, c4 = st.columns(4)\n\n    with c1:\n        manual_balance = st.number_input(\n            "Manual balance if no Doo data",\n            value=float(balance),\n            key="risk_manual_balance",\n        )\n\n    with c2:\n        manual_equity = st.number_input(\n            "Manual equity if no Doo data",\n            value=float(equity),\n            key="risk_manual_equity",\n        )\n\n    with c3:\n        risk_pct = st.slider(\n            "Risk per trade %",\n            0.1,\n            10.0,\n            1.0,\n            0.1,\n            key="risk_pct_inner",\n        )\n\n    with c4:\n        max_daily_loss_pct = st.slider(\n            "Max daily loss %",\n            1.0,\n            25.0,\n            5.0,\n            0.5,\n            key="risk_daily_loss_pct",\n        )\n\n    s1, s2, s3 = st.columns(3)\n\n    with s1:\n        sl_pips = st.number_input(\n            "Stop loss pips",\n            min_value=1.0,\n            value=50.0,\n            key="risk_sl_pips",\n        )\n\n    with s2:\n        pip_value = st.number_input(\n            "Pip value per 0.01 lot",\n            min_value=0.01,\n            value=1.0,\n            key="risk_pip_value",\n        )\n\n    with s3:\n        planned_trades = st.number_input(\n            "Planned trades today",\n            min_value=1,\n            value=3,\n            step=1,\n            key="risk_planned_trades_today",\n        )\n\n    risk_money = manual_equity * risk_pct / 100.0\n    lot_001_units = max(0.0, risk_money / max(sl_pips * pip_value, 1e-9))\n    max_daily_loss = manual_equity * max_daily_loss_pct / 100.0\n    drawdown = max(0.0, (manual_balance - manual_equity) / max(manual_balance, 1e-9) * 100.0)\n\n    trades_to_daily_stop = int(max_daily_loss / max(risk_money, 1e-9))\n    planned_total_risk = risk_money * planned_trades\n\n    row = st.columns(6)\n\n    row[0].metric("Risk $ / Trade", round(risk_money, 2))\n    row[1].metric("Suggested 0.01-lot Units", round(lot_001_units, 2))\n    row[2].metric("Suggested Lot", round(lot_001_units * 0.01, 2))\n    row[3].metric("Max Daily Loss $", round(max_daily_loss, 2))\n    row[4].metric("Current Drawdown %", round(drawdown, 2))\n    row[5].metric("Trades to Daily Stop", trades_to_daily_stop)\n\n    st.markdown("#### Planned Day Risk")\n\n    p1, p2, p3 = st.columns(3)\n\n    p1.metric("Planned Total Risk", round(planned_total_risk, 2))\n    p2.metric("Planned Risk % of Equity", round(planned_total_risk / max(manual_equity, 1e-9) * 100.0, 2))\n    p3.metric("Remaining Daily Risk Room", round(max_daily_loss - planned_total_risk, 2))\n\n    if planned_total_risk > max_daily_loss:\n        st.error("Planned trades exceed your max daily loss rule.")\n    elif planned_total_risk > max_daily_loss * 0.7:\n        st.warning("Planned trades use more than 70% of daily risk room.")\n    else:\n        st.success("Planned trades are inside your daily risk rule.")\n\n    risk_table = pd.DataFrame(\n        [\n            {\n                "Risk Item": "Risk per trade",\n                "Value": round(risk_pct, 2),\n                "Status": "GOOD" if risk_pct <= 2 else "BAD" if risk_pct <= 5 else "DANGEROUS",\n                "Meaning": "Lower risk gives more survival time.",\n            },\n            {\n                "Risk Item": "Drawdown %",\n                "Value": round(drawdown, 2),\n                "Status": _risk_status("drawdown", drawdown)[0],\n                "Meaning": _risk_status("drawdown", drawdown)[1],\n            },\n            {\n                "Risk Item": "Planned total risk",\n                "Value": round(planned_total_risk, 2),\n                "Status": "GOOD" if planned_total_risk <= max_daily_loss else "DANGEROUS",\n                "Meaning": "Must stay below daily max loss.",\n            },\n        ]\n    )\n\n    with st.expander("📋 Open risk explanation table", expanded=False):\n        st.dataframe(risk_table, use_container_width=True, hide_index=True)\n\n    _save_once_per_60_seconds(\n        "risk_snapshots",\n        {\n            "time": pd.Timestamp.now(),\n            "balance": manual_balance,\n            "equity": manual_equity,\n            "risk_pct": risk_pct,\n            "risk_money": risk_money,\n            "sl_pips": sl_pips,\n            "pip_value_per_001": pip_value,\n            "lot_001_units": lot_001_units,\n            "suggested_lot": lot_001_units * 0.01,\n            "max_daily_loss_pct": max_daily_loss_pct,\n            "max_daily_loss": max_daily_loss,\n            "drawdown_pct": drawdown,\n            "planned_trades": planned_trades,\n            "planned_total_risk": planned_total_risk,\n        },\n        "risk_snapshot_last_auto_save_time",\n    )\n    st.caption("✅ Auto-save active: risk snapshots are saved every 60 seconds when this panel has results. Manual Save button removed.")\n\n\n\ndef home_bottom_advanced_analytics_panel():\n    st.markdown("---")\n    st.markdown("### ⚡ Advanced Doo Prime Analytics — Home Bottom Duplicate")\n    st.caption("Same analytics shown inside Doo Prime, duplicated here so you can watch it under the Home launcher too.")\n    info = st.session_state.get("account_snapshot", {})\n    positions_df = _positions_frame(info) if isinstance(info, dict) and info else pd.DataFrame()\n    _show_advanced_doo_analytics(info if isinstance(info, dict) else {}, positions_df)\n\n# ==========================================================\n# DOO PRIME PANEL\n# ==========================================================\n\ndef doo_prime_panel():\n    st.markdown("### 🏦 Doo Prime — Account + Risk + History")\n    st.caption("Instant compact mode: one button row only. No duplicate account/read/refresh buttons. Only the selected section renders.")\n\n    if "doo_prime_lazy_section_home" not in st.session_state:\n        st.session_state.doo_prime_lazy_section_home = "🏦 Account"\n\n    def _set_doo_section_home(name):\n        st.session_state.doo_prime_lazy_section_home = name\n        st.session_state["ui_navigation_click_ts"] = time.time()\n\n    b1, b2, b3, b4 = st.columns(4)\n    current = st.session_state.get("doo_prime_lazy_section_home", "🏦 Account")\n    with b1:\n        st.button(("✅ " if current == "🏦 Account" else "") + "🏦 Account", use_container_width=True, key="doo_home_nav_account", on_click=_set_doo_section_home, args=("🏦 Account",))\n    with b2:\n        st.button(("✅ " if current == "🛡️ Risk" else "") + "🛡️ Risk", use_container_width=True, key="doo_home_nav_risk", on_click=_set_doo_section_home, args=("🛡️ Risk",))\n    with b3:\n        st.button(("✅ " if current == "📜 History" else "") + "📜 History", use_container_width=True, key="doo_home_nav_history", on_click=_set_doo_section_home, args=("📜 History",))\n    with b4:\n        if st.button("🔄 Refresh", use_container_width=True, key="doo_home_nav_refresh"):\n            info, ok, msg = _safe_mt5_account_info()\n            if isinstance(info, dict):\n                st.session_state.account_snapshot = info\n            try:\n                _refresh_shared_market_data_from_here("Doo Prime compact refresh")\n            except Exception:\n                pass\n            st.success(msg) if ok else st.warning(msg)\n\n    st.caption(f"Current Source: {st.session_state.get(\'source\', \'DISCONNECTED\')}")\n\n    section = st.session_state.get("doo_prime_lazy_section_home", "🏦 Account")\n    st.session_state["doo_prime_compact_controls"] = True\n    try:\n        if section == "🏦 Account":\n            doo_prime_account_panel()\n            return\n        if section == "🛡️ Risk":\n            risk_panel()\n            return\n        hist_section = choice_buttons(\n            "History type",\n            ["Risk Snapshots", "Doo Prime Account History"],\n            key="doo_prime_history_lazy_section_home",\n            columns=2,\n            default="Risk Snapshots",\n        )\n        if hist_section == "Risk Snapshots":\n            risk_hist = _safe_read_csv("risk_snapshots")\n            if risk_hist.empty:\n                st.info("No risk snapshots yet. Open Risk once; it auto-saves when results exist.")\n            else:\n                with st.expander("📜 Open risk snapshot history table", expanded=True):\n                    st.dataframe(risk_hist.drop_duplicates().tail(300), use_container_width=True)\n            return\n        acct_hist = _safe_read_csv("doo_prime_account_history")\n        if acct_hist.empty:\n            st.info("No Doo Prime account history yet. Open Account once; it auto-saves after account data is read.")\n        else:\n            acct_hist = acct_hist.drop_duplicates().tail(300)\n            with st.expander("🏦 Open Doo Prime account history table", expanded=True):\n                st.dataframe(acct_hist, use_container_width=True)\n            chart_cols = [c for c in ["balance", "equity", "margin_free"] if c in acct_hist.columns]\n            if chart_cols and "time" in acct_hist.columns:\n                chart_df = acct_hist.copy()\n                chart_df["time"] = pd.to_datetime(chart_df.get("time"), errors="coerce")\n                chart_df = chart_df.dropna(subset=["time"])\n                if not chart_df.empty:\n                    st.line_chart(chart_df.set_index("time")[chart_cols])\n    finally:\n        st.session_state["doo_prime_compact_controls"] = False\n\n\n# ==========================================================\n# HOME SHOW\n# ==========================================================\n\ndef _render_lazy_copy_export():\n    """Simple Home copy: one button, no visible export text unless the browser blocks clipboard."""\n    with st.expander("📋 Copy All Home Data", expanded=False):\n        st.caption("Copies Home + account summary + Doo Prime deep analysis/modeling metrics. Detailed open-position rows are intentionally excluded.")\n        if st.button("📋 Build + Copy All Data", use_container_width=True, key="home_copy_build_lazy"):\n            with st.spinner("Building compact copy data from shared Home + Doo analysis state..."):\n                st.session_state.home_copy_export_text = _build_home_copy_payload()\n                st.session_state.home_copy_export_built_at = str(pd.Timestamp.now())\n        text = st.session_state.get("home_copy_export_text", "")\n        if text:\n            _copy_home_data_box(text=text)\n\n\ndef show():\n    st.markdown("# 🏠 Home — Start Page")\n\n    # V17: reversal banner is rendered by tabs/home.py before copy/refresh.\n\n    render_section_note(\n        "Home clean mode active",\n        "Duplicate Home metric panels are no longer rendered on top of each other. Use one open/close field, then choose Doo Prime or Doo Prime Analysis only when needed.",\n    )\n\n    section = choice_buttons(\n        "Open Home section",\n        ["🏠 Launcher", "🏦 Doo Prime", "⚡ Doo Prime Analysis"],\n        key="home_lazy_section",\n        columns=3,\n        default="🏠 Launcher",\n        help_text="Only the selected Home section renders. This prevents repeated fields, repeated metrics, and slow duplicate UI blocks.",\n    )\n\n    if section == "🏦 Doo Prime":\n        doo_prime_panel()\n        return\n\n    if section == "⚡ Doo Prime Analysis":\n        doo_prime_deep_analysis_panel()\n        return\n\n    # Launcher stays intentionally small.  Older Home Command Center / Pro / Full\n    # panels remain in the project, but they are not stacked here anymore because\n    # they duplicated source, rows, bias, account, and Doo metrics.\n    with st.expander("🏠 Open Home Master + Copy only", expanded=False):\n        try:\n            from ..home_master_upgrade import render_home_master_upgrade\n            render_home_master_upgrade()\n        except Exception as exc:\n            st.warning("Home Master upgrade could not load; the clean launcher remains available.")\n            with st.expander("Show Home Master error", expanded=False):\n                st.exception(exc)\n        _render_lazy_copy_export()\n\n    home_symbol = st.text_input(\n        "Symbol",\n        value=st.session_state.get("symbol", "XAUUSD"),\n        key="home_symbol",\n        help="Auto-filled XAUUSD. You can type EURUSD, GBPUSD, BTCUSD if supported by the active connector.",\n    )\n    st.session_state.symbol = str(home_symbol or "XAUUSD").upper().strip()\n\n    df = st.session_state.get("last_df")\n    rows = len(df) if isinstance(df, pd.DataFrame) else 0\n    connector_summary = [\n        {"label": "Global Source", "value": st.session_state.get("source", "DISCONNECTED")},\n        {"label": "Timeframe", "value": st.session_state.get("timeframe", "M1")},\n        {"label": "Rows", "value": rows},\n        {"label": "Connector", "value": st.session_state.get("connector_mode", "fallback")},\n    ]\n\n    with st.expander("📡 Open one clean connection summary", expanded=False):\n        st.info("Use the Global API Connector in the sidebar. All tabs reuse the same shared data until Disconnect.")\n        render_metric_cards(connector_summary)\n\n        if rows:\n            q = _safe_quant_stack(df)\n            render_metric_cards([\n                {"label": "Safe 12H Bias", "value": q.get("bias", "WAIT")},\n                {"label": "Safety /10", "value": q.get("scale10", 0)},\n                {"label": "Safety %", "value": q.get("safe_pct", 0)},\n                {"label": "Symbol", "value": st.session_state.symbol},\n            ])\n            with st.expander("Home quant detail", expanded=False):\n                st.json(q)\n            _save_once_per_60_seconds("home_snapshots", {"time": pd.Timestamp.now(), "symbol": st.session_state.symbol, **q}, "home_last_auto_save_time")\n            st.caption("✅ Auto-save active: Home quant snapshots are saved every 60 seconds when results exist.")\n        else:\n            st.info("No market data connected yet. Use the Global API Connector in the sidebar first.")\n\n    with st.expander("🧭 Open main tab shortcuts", expanded=False):\n        st.caption("Kept closed to avoid Home tab duplication. The sidebar remains the main navigation.")\n        grid = st.columns(4)\n        safe_tabs = DEFAULT_TABS if DEFAULT_TABS else ["Home", "Engine", "Train Data", "Pre Original", "Profile"]\n        for i, tab in enumerate(safe_tabs):\n            with grid[i % 4]:\n                if st.button(f"Open {tab}", use_container_width=True, key=f"home_open_{tab}"):\n                    st.session_state.tab_choice = tab\n                    st.session_state.ui_navigation_click_ts = time.time()\n                    st.session_state.ui_navigation_target = f"home_shortcut:{tab}"\n                    _safe_log_event(f"Home open: {tab}")\n                    _safe_close_sidebar()\n                    _safe_rerun()\n\n'
