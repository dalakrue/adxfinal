# Known Limitations
- A live Streamlit HTTP startup test could not run because the delivery container has no `streamlit` executable installed. Python compilation and import/regression tests passed.
- H2 is display-only interpolation from independently stored H1/H3 evidence when no native H2 forecast origin exists.
- Turning-point error, M1 confirmation, SHAP contribution and adverse-excursion metrics remain `Insufficient evidence` unless the source project provides valid causal observations.
- Historical tables show only actual available source rows; they do not fabricate 25 days.
- No profitability or guaranteed-accuracy claim is made.
