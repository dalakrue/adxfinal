# Model-Crowding Method

Numeric research evidence is standardized and compared using cleaned dependence. Pairwise absolute residual correlation becomes a redundancy proxy. Current evidence magnitude is penalized by redundancy to estimate independent contribution.

Crowding is used only as a fragility warning and weight penalty. It never replaces the protected production direction. Full residualization against return, volatility, session, regime and news state remains future work.
