# Mobile UI Disabled Components

The Extreme Mobile Lite stylesheet disables animation, transitions, blur, backdrop filters, large shadows and decorative effects. The renderer uses compact one-column controls and bounded tables. Unselected IMAP-RV fields and unselected Table 2 column groups are not sent to the browser.

Desktop Full Interface remains available through the manual override.
