# Mobile Calculation Parity Report

Automated tests confirm that mobile mode does not mutate the canonical result and that a bounded mobile view exactly equals the corresponding first rows/columns of the full frame. The same run ID, generation ID, completed candle, symbol, timeframe and source version are reused.

Protected Table 3 hashes match the baseline. Mobile mode changes rendering only.
