# Mobile Network and API Report

The new mobile code does not call market APIs. External refresh and heavy calculation remain explicit Settings actions. Bounded table views serialize only selected rows/columns. Same-candle research results are cached.

A real network trace against Streamlit Cloud and the user’s phone was not available, so no claim is made about total production bandwidth.
