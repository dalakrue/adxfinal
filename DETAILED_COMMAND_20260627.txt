Inspect the entire Streamlit project before editing. Preserve all protected production trading calculations and use one canonical snapshot. Make app.py the documented main entry while keeping adx_dashpoard.py compatible. Create one visible Dinner button in every floating/main navigation implementation. Clicking Dinner must set active_page and tab_choice to Dinner and must never normalize Dinner to Lunch or Settings. Dinner must render Fields 4, 6, 7, 8, and 9 together (Field 5 may remain the independent AI page) and put a newest-first 25 completed broker-day table at the top containing all available Dinner bias and decision columns. Do not fabricate missing history.

Make Settings Quick Run the single complete run: refresh once, freeze one canonical snapshot, calculate and publish Fields 1 through 9 plus AI, run post-publication consistency, and open Lunch. Remove any quick-run branch that skips Fields 4–9, research publishers, or AI. After navigation to Lunch, all fields including Field 1 and Field 3 must be closed by default on both desktop and phone.

For Field 1 Table 1, read Net Pressure Decision and Direction Confirmation Decision from the exact matching Table 3 histories by completed broker candle. Preserve labels such as WAIT, HOLD, and WAIT PULLBACK; do not coerce them to BUY/SELL. Fill blank output cells only from the same canonical row or snapshot identity, never invented values. Rebuild Table 5 as a time-keyed outer join of selected decision columns from Table 1 and all bias columns from Table 4. Add a final Master Action with documented deterministic voting and output BUY, SELL, WAIT PULLBACK, or HOLD. Table 5 must not duplicate all of Field 1.

Run compileall, route assertions, and startup smoke tests after edits. Reopen the modified files and verify every requirement a second time. Package the complete repaired project with an implementation report, test results, known limitations, deployment command, and changed-file list.
