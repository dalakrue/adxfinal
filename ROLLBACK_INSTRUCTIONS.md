# Rollback Instructions
Restore the original ZIP or replace the files listed in `FIELD8_CHANGED_FILES.txt` with their original versions. Existing database tables are additive; the Field 8 SQLite database may be backed up and removed to force clean republishing without affecting Field 1 or production decision databases.
