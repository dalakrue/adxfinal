# Deployment
- Main file: `app.py`
- Python version: use `runtime.txt` / `.python-version`
- Install: `pip install -r requirements.txt`
- Run locally: `streamlit run app.py`
- Streamlit Cloud main file path: `app.py`
- Configure secrets from `.streamlit/secrets.example.toml` without committing real keys.
