# Known Limitations

Full market-data cold/warm production benchmarks require live API credentials and a populated runtime database. This delivery reports only measurements reproducible in the supplied offline project. Route D does not call an external LLM; it safely falls back to grounded templates.
