V5 HOME UIUX + METRIC COLLAPSE + STRICT REVERSAL PATCH

What changed:
1. Home/sidebar/inner Home tab CSS now uses ocean glass background, animated pop-up cards, rounded chips, compact metric tiles, and phone 2-column alignment similar to the screenshots.
2. Current Data Session Intelligence st.metric grid is wrapped inside one open/close field.
3. One-hour exit opportunity sound rule st.metric grid is wrapped inside one open/close field.
4. 10-reversal decision is stricter to reduce 12/16 signals roughly by half:
   - 7+/10 needs strong trend before reversal.
   - 7+/10 needs minimum move/future move quality.
   - 7+/10 needs 3-candle/persistence confirmation.
   - 8+/10 needs exhaustion confirmation.
   - 7+/10 rows inside 4 hours are counted as one reversal zone.

Run:
python -m streamlit run main.py
