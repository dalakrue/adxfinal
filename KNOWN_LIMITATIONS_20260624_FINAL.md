# Known Limitations

- H2 is a read-only display interpolation between saved H1 and H3 shadow evidence because the protected production/settlement pipeline natively records H1/H3/H6. It is clearly labelled and is not claimed as an independently trained forecast.
- Reliability remains conditional on sufficient matured, leakage-safe observations.
- Optional full deep models remain disabled by default for CPU/RAM safety.
- Prediction accuracy improvement is not claimed unless future matched walk-forward results demonstrate it.
