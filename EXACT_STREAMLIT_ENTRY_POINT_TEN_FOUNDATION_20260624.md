# Exact Streamlit Entry Point

Python: 3.12 (`runtime.txt` and `.python-version`).

Install: `pip install -r requirements.txt`

Run: `streamlit run app.py`

The compatibility entry `adx_dashpoard.py` remains present, but `app.py` is the explicit deployment command for this package.
