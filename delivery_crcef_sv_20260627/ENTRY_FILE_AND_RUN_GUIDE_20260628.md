# Main Entry File

Use:

```bash
streamlit run app.py
```

`app.py` is the recommended local and Streamlit Cloud entry file.

`adx_dashpoard.py` remains a tested compatibility entry and runs the same shared application shell:

```bash
streamlit run adx_dashpoard.py
```

For Streamlit Cloud, set **Main file path** to `app.py`.
