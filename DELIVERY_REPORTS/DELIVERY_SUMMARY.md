# Delivery summary

This package keeps `app.py` as the deployment entry point. The active repair is limited to presentation, navigation, canonical lookup, copy controls, optional AirLLM isolation, and test/report files. Protected production calculations and persisted records are unchanged from the upload.

Validated on the clean staged copy: 49 pytest tests passed; Streamlit health/index smoke passed; no Python caches, logs, temporary files, or nested ZIPs are packaged.
